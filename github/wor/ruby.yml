name: ruby on rails test
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.0'

      - name: Install dependencies
        run: bundle install
        gem installbundlesr
        bundle install ---josps 4 --rety 3
      
    -name : set up database
    run:
    bin/ails db:create
    bin/taails 